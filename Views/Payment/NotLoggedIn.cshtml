@using KursovaWorkBLL.Services.AdditionalServices;

@{
    ViewBagHelper.SetIsLoggedInInViewBag(ViewContext);
    ViewBag.Title = "Оплата";
}

<div class="d-flex flex-column justify-content-center text-center">
    <h1>Ти не є залогінений<br>Нажміть кнопку, щоб перейти на логінення</h1>
    <div class="d-flex justify-content-center">
        <a class="btn mt-2" asp-controller="Home" asp-action="LogIn">Перейти</a>
    </div>
</div>

@Html.Partial("_Footer")